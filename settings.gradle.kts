pluginManagement {
    repositories {
        mavenLocal()
        maven {
            url = uri("/Volumes/disk/git/kmp/kotlin22-ohos/build/repo")
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "KotlinNativeTemplate"